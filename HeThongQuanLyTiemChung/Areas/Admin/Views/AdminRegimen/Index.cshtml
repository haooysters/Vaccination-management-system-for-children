@model IEnumerable<HeThongQuanLyTiemChung.Models.Regimen>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header">
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Trang chủ</a>
            <span class="breadcrumb-item active">Danh sách vắc xin theo phác đồ</span>
        </nav>
    </div>
</div>

<div class="row m-b-30">
    <div class="col-lg-8">
        <a class="btn btn-primary" asp-area="Admin" asp-controller="AdminRegimen" asp-action="Create"><i class="anticon anticon-plus-circle m-r-5"></i><span>Thêm mới</span></a>
    </div>
    <div class="col-lg-4 ">

    </div>
</div>
<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th scope="col">ID</th>
                <th scope="col">Tên vaccine</th>
                <th width="100px" scope="col">Mũi tiêm</th>
                <th width="120px" scope="col">Tháng tuổi</th>
                <th scope="col">#</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null)
            {
                foreach (var item in Model)
                {
                    <tr>
                        <th scope="row">@item.RegimenId</th>
                        <td>@item.Vaccine.VaccineName</td>
                        <td>@item.Injection.InjectionName</td>
                        <td>@item.MonthAge.MonthAgeName</td>
                        <td width="280px">
                            <a class="btn btn-primary btn-tone m-r-5" asp-area="Admin" asp-controller="AdminRegimen" asp-action="Details" asp-route-id="@item.RegimenId">Chi tiết</a>
                            <a class="btn btn-secondary btn-tone m-r-5" asp-area="Admin" asp-controller="AdminRegimen" asp-action="Edit" asp-route-id="@item.RegimenId">Sửa</a>
                            <a class="btn btn-success btn-tone m-r-5" asp-area="Admin" asp-controller="AdminRegimen" asp-action="Delete" asp-route-id="@item.RegimenId">Xóa</a>

                        </td>
                    </tr>
                }
            }

        </tbody>
    </table>
</div>
