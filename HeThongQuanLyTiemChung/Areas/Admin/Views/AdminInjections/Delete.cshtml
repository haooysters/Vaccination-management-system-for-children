@model HeThongQuanLyTiemChung.Models.Injection

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="page-header">
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Trang chủ</a>
            <a asp-area="Admin" asp-controller="AdminInjections" asp-action="Index" class="breadcrumb-item">Danh sách mũi tiêm</a>
            <span class="breadcrumb-item active">Xác nhận xóa mũi tiêm</span>
        </nav>
    </div>
</div>

<div class="card-body">
    <h4 class="card-title">Xác nhận xóa mũi tiêm</h4>
    <div class="table-responsive">
        <table class="product-info-table m-t-20">
            <tbody>
                <tr>
                    <td>ID:</td>
                    <td class="text-dark font-weight-semibold">@Model.InjectionId</td>
                </tr>
                <tr>
                    <td>Tên bệnh:</td>
                    <td>@Model.Disease.DiseaseName</td>
                </tr>
                <tr>
                    <td>Tên mũi tiêm:</td>
                    <td>@Model.InjectionName</td>
                </tr>
                <tr>
                    <td>Tháng tuổi:</td>
                    <td>@Model.MonthAgeName</td>
                </tr>
                <tr>
                    <td>Xác nhận xóa:</td>
                    <td>
                        <form asp-action="Delete" asp-route-id="@Model.InjectionId">
                            <input type="hidden" asp-for="InjectionId" />
                            <input type="submit" value="Xác nhận xóa" class="btn btn-danger" />
                        </form>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>